{
  "name": "@mycel/rally",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local multi-seat LinkedIn rally runner. Runs on the founder's laptop, on the founder's own residential IP, driving one real Chrome profile per account at human pace. Deliberately separate from the outbound product: different risk model, different machine, different lifetime.",
  "bin": {
    "rally": "./src/cli.ts"
  },
  "scripts": {
    "rally": "node --experimental-sqlite --import tsx src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-sqlite --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@mycel/linkedin": "file:../linkedin",
    "@types/pg": "^8.23.1",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  },
  "optionalDependencies": {
    "playwright-core": "^1.62.1"
  }
}
